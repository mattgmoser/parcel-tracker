{
  "name": "parcel-tracker",
  "version": "1.0.0",
  "description": "A small HTTP service for tracking parcels through a delivery network.",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test --test-timeout=30000 dist/test/*.test.js",
    "start": "node dist/src/server.js"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "typescript": "5.7.2"
  }
}
